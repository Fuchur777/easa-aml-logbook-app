# Copy to keystore.properties (gitignored) and fill in. Paths are relative to the repo root.
# Never commit the real file or the .jks it points at — losing either means you can never
# update the app on Play again under the same listing.
storeFile=upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
